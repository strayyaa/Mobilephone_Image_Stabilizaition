# PSD分析方法对比

## � 核心差异

### **问题：为什么两个结果差这么多？**

**同学的图**: 横轴 10^-3 到 10^-1 Hz，曲线锯齿状  
**我的图**: 横轴 0.5 到 50 Hz，曲线平滑

---

## ⚠️ 三个关键错误

### **1. 采样率错误（最严重）**

```python
# 同学的代码
sample_spacing = 1.0  # ❌ 错误！意味着每秒1个数据点

# 实际情况
时间间隔 = 0.002秒  # 每秒500个数据点
```

**影响**:
- 错误: 频率范围 0-0.5 Hz（太慢，不合理）
- 正确: 频率范围 0-250 Hz（合理的陀螺仪频率）
- **错了500倍！**

---

### **2. 方法不同**

| 特性 | 同学（周期图法） | 我（Welch方法） |
|------|----------------|----------------|
| 分段 | 无 | 512点/段 |
| 平均 | 无 | 12段平均 |
| 结果 | 波动大，有毛刺 | 平滑稳定 |
| 标准 | 快速粗略 | 工程标准 ✓ |

**简单理解**:
- 周期图 = 测量1次 → 不稳定
- Welch = 测量12次取平均 → 稳定

---

### **3. 预处理不完整**

```python
# 同学的代码
centered = series - np.mean(series)  # 只去均值

# 我的代码
signal = detrend(signal, type='linear')  # 去线性趋势
signal = (signal - mean) / std           # 标准化
```

---

## ✅ 结论

**我的实现是正确的**，因为：
1. ✓ 采样率正确（500 Hz）
2. ✓ 使用Welch方法（标准做法）
3. ✓ 完整预处理

**同学的主要问题**：
1. ✗ 采样率错误（1 Hz，应该是500 Hz）
2. ✗ 直接FFT不稳定
3. ✗ 缺少预处理

---

## � 快速修复（给同学）

只需改一行：

```python
# 原来
sample_spacing = 1.0

# 改成
sample_spacing = df['time'].diff().mean()  # 自动计算，约0.002
```

更好的做法：改用Welch方法

```python
from scipy.signal import welch

fs = 1 / sample_spacing  # 约500 Hz
freqs, psd = welch(signal, fs=fs, nperseg=512, noverlap=256)
```
