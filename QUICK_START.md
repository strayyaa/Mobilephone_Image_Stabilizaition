# 频谱分析与DCT增强 - 操作指南

## 🔬 核心原理

### 功率谱密度(PSD)分析做什么？

**关键步骤**:
1. **去趋势**: 去除信号的缓慢漂移（线性趋势）
2. **标准化**: 将信号归一化到均值0、标准差1
3. **Welch方法**: 将信号分段，计算每段的频谱，然后平均
4. **输出**: 频率-功率密度曲线

**物理意义**:
- 横轴（频率）: 信号变化的快慢（Hz = 每秒多少次）
- 纵轴（功率密度）: 该频率的能量大小
- 峰值位置 = 信号的主要频率成分

---

### DCT增强做什么？

**关键步骤**:
1. **DCT变换**: 将时域信号转换到频域（类似傅里叶变换）
2. **系数加权**: 
   - 低频系数 × 1.3（增强慢变化）
   - 高频系数 × 0.2（抑制快变化/噪声）
3. **截断**: 只保留前70%的系数（去除最高频噪声）
4. **逆DCT**: 转换回时域信号

**物理意义**:
- 保留真实运动（低频）
- 去除传感器噪声（高频）
- 得到更干净的信号

---

## 📋 简单三步走

### 第一步：测频谱密度

**运行命令**:
```bash
python test_psd.py
```

**会发生什么**:
- 读取陀螺仪数据（第6000-12000行）
- 分析三个角度通道的频谱特性
- 生成结果到 `./psd_results/` 文件夹

**查看结果**:
- `psd_analysis.png` - 三个通道的频谱图
- `psd_report.txt` - 文字分析报告

**看什么**:
- 图上的峰值位置 = 主要频率
- 报告里的能量分布 = 低/中/高频占比

---

### 第二步：测DCT增强效果

**运行命令**:
```bash
python test_dct.py
```

**会发生什么**:
- 对三个角度通道应用DCT增强
- 对比原始信号 vs 增强信号
- 生成对比图到 `./dct_results/` 文件夹

**查看结果**:
- `x_angle_1_comparison.png` - X轴对比
- `y_angle_1_comparison.png` - Y轴对比  
- `z_angle_1_comparison.png` - Z轴对比

**看什么**:
- 上图：时域波形，看信号是否更平滑
- 下图：DCT系数，看高频噪声是否被抑制

---

### 第三步：调整参数（可选）

如果效果不理想，修改 `test_dct.py` 中的参数：

```python
enhancer = DCTEnhancer(
    low_freq_boost=1.3,      # 改这个：低频增强倍数（1.0-2.0）
    high_freq_suppress=0.2,  # 改这个：高频保留比例（0.1-0.5）
    cutoff_ratio=0.7         # 改这个：总体保留比例（0.5-0.9）
)
```

**参数说明**:
- `low_freq_boost` 越大 → 低频越强（慢变化更明显）
- `high_freq_suppress` 越小 → 高频越弱（噪声越少）
- `cutoff_ratio` 越小 → 信号越平滑（但可能丢信息）

---

## 📊 结果图片怎么看？

### PSD分析图 (psd_analysis.png)

**图片结构**: 三个子图，分别对应 x_angle_1, y_angle_1, z_angle_1

**横轴**: 频率 (Hz)
- 0-2 Hz: 低频区（缓慢运动）
- 2-20 Hz: 中频区（抖动）
- 20-50 Hz: 高频区（噪声）

**纵轴**: 功率谱密度（对数刻度）
- 数值越大 = 该频率的能量越强
- 峰值 = 主要频率成分

**红色虚线**: 标注的主导频率位置

**怎么判断**:
```
好的信号:
  ✓ 低频区有明显峰值（真实运动）
  ✓ 高频区功率很低（噪声小）
  
需要处理的信号:
  ✗ 高频区功率高（噪声大）
  ✗ 中频区有很多峰值（抖动严重）
```

---

### DCT对比图 (x/y/z_angle_1_comparison.png)

**上图 - 时域对比**:
- 蓝线: 原始信号
- 橙线: DCT增强后的信号

**看什么**:
- 增强后的信号应该更平滑
- 高频抖动应该减少
- 整体趋势保持不变

**下图 - DCT系数对比**:
- 横轴: 系数索引（0=最低频，越大越高频）
- 纵轴: 系数幅度（对数刻度）

**看什么**:
```
左侧（低频系数）:
  - 橙线应该略高于蓝线（增强了）
  
右侧（高频系数）:
  - 橙线应该明显低于蓝线（抑制了）
  - 最右侧应该接近0（截断了）
```

**理想效果**:
- 时域图: 橙线比蓝线更平滑，但保持相同的大趋势
- 频域图: 橙线呈现"左高右低"的趋势

---

## 🎯 快速理解

### PSD告诉你什么？
```
低频能量高 → 有缓慢的运动（正常）
中频能量高 → 有抖动（可能需要处理）
高频能量高 → 有噪声（需要滤除）
```

### DCT做了什么？
```
保留低频 → 保留真实运动
抑制高频 → 去除噪声
结果 → 更干净的信号
```

### 数值示例

**PSD报告中的数值**:
```
x_angle_1:
  主导频率: 3.52 Hz        ← 主要运动频率
  总功率: 0.123456         ← 总能量
  低频能量: 65.3%          ← 大部分是真实运动 ✓
  中频能量: 28.1%          ← 有一些抖动
  高频能量: 6.6%           ← 噪声较少 ✓
```

**好的信号特征**:
- 低频能量 > 50%
- 高频能量 < 15%
- 主导频率在 0-10 Hz 范围内

---

## ❓ 常见问题

**Q: 运行出错怎么办？**
A: 检查 `./dataset/data.csv` 文件是否存在

**Q: 图看不懂怎么办？**
A: 
- PSD图：峰越高 = 该频率能量越大
- DCT图：增强后的曲线应该更平滑

**Q: 参数怎么选？**
A: 先用默认值，看效果不好再调

---

## 📝 下一步

如果DCT增强效果好，可以集成到训练流程：
1. 修改 `data_provider/data_loader.py`
2. 在 `Dataset_Angle` 类中添加DCT增强选项
3. 重新训练模型对比效果

（具体集成方法见 `implementation_plan.md`）
