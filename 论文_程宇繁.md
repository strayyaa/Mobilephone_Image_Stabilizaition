# 程宇繁论文部分

## 第二章 项目展示与效果总览

### 2.1 本地Web平台

本项目开发了一套完整的本地Web平台，用于验证和展示手机与无人机抖动预测及稳像效果。平台采用前后端分离架构，后端基于Python Flask框架提供RESTful API接口，前端使用Vue.js构建交互式可视化界面。平台核心功能包括：原始抖动序列上传与预处理、模型推理与预测、可视化轨迹对比、稳像效果导出等模块。

用户通过平台可直观地观察输入信号与预测结果之间的差异，系统自动计算稳像指标并生成评估报告。平台支持批量处理模式，用户可一次性上传多个样本文件进行批量推理，极大提升了实验验证效率。此外，平台集成了模型参数配置面板，支持动态调整预测步长、补偿策略等关键参数，无需修改代码即可完成不同配置下的效果对比。

### 2.2 手机稳像效果展示

针对手机采集的抖动数据，本系统实现了有效的稳像处理。原始抖动信号主要表现为低频大幅度的角度偏移，频率分析显示99.99%的信号能量集中在0至10Hz的低频波段，几乎不包含中高频成分。这一特性使得传统的基于高频噪声抑制的稳像方法难以直接应用。

经过MoE混合专家模块处理后，预测误差得到显著抑制。在标准化指标下，基于PatchTST主干的MoE增强模型将MAE从0.0978降低至0.0939，相对降幅达到4.0%。更进一步的freqMOE单专家配置（仅使用FFT域归一化而未进行频段划分）将MAE降低至0.0892，相对基准提升8.8%。经更强数据增强优化后，最优配置达到MAE=0.0841，相较基准提升14.1%，反标准化MAE达到0.0349。这一结果验证了针对低频抖动信号的专用处理策略的有效性。

稳像效果的视觉展示通过轨迹对比图呈现。原始输入轨迹呈现明显的抖动模式，幅值可达数十度；经预测补偿后的输出轨迹明显平滑，保留了原始运动趋势的同时有效抑制了高频抖动成分。系统同时输出置信度指标，用于评估单次预测的可靠性，当预测不确定性较高时自动标记异常段落供人工复核。

### 2.5 可解释输出设计

为提升模型输出的可解释性并便于实际应用中的质量控制，本系统设计了多维度的可解释输出模块。首先是轨迹可视化模块，系统自动绘制原始信号与预测信号的叠加对比图，不同颜色区分输入输出轨迹，异常段落使用特殊标记高亮显示。其次是裁切窗口计算模块，根据预测的抖动幅度自动计算最优的图像裁切范围，输出裁切矩阵供下游图像处理使用。

置信度评估模块基于预测信号的能量分布与历史预测误差统计，为每个预测样本生成0至1之间的置信度得分。当置信度低于预设阈值时，系统将该样本标记为“需人工复核”，并输出异常段落的起止位置供后续分析。导出模块支持多种格式输出，包括JSON格式的数值矩阵、CSV格式的统计报告以及PNG格式的可视化图像，满足不同应用场景的需求。

## 第三章 系统架构与关键模块

### 3.1 总体架构设计

本系统采用模块化的分层架构设计，从数据输入到稳像输出形成清晰的数据流。整体架构分为四个层次：数据层负责原始信号采集与预处理；模型层实现抖动预测的核心算法；补偿层完成轨迹平滑与稳像生成；应用层提供可视化与导出功能。

数据流的具体路径如下：原始时序信号首先进入预处理模块，完成去噪、归一化与异常段检测；预处理后的信号送入预测主干网络（PatchTST/DLinear/ModernTCN），同时可选地经过MoE增强模块或freqMOE增强模块进行频域特征优化；预测结果与原始信号一同输入反向补偿模块，结合裁切策略生成最终的稳像输出。各模块之间通过标准化的数据接口连接，支持灵活的配置替换与功能扩展。

### 3.2 数据与预处理

#### 3.2.1 手机自建数据集

手机抖动数据集通过在Android与iOS设备上自主采集获得。原始数据包含三轴陀螺仪角速度（omega）与三轴角度序列（angle），采样率为500Hz。

数据集规模为16830条记录，时间跨度约33秒。数据预处理首先对角速度信号进行积分得到角度序列，随后进行时间对齐与异常值剔除。

**频率特性分析**显示，该数据集呈现出显著的低频主导特征：对全部样本的FFT分析结果表明，99.99%的信号能量集中在0至10Hz的低频波段，10至50Hz的中频波段仅占0.01%，50Hz以上的高频波段几乎可以忽略（0.00000006%）。这一独特的频率分布对后续的模型设计具有重要指导意义：传统的多频段划分策略在该数据上难以发挥效果基于，而FFT域归一化的单专家配置反而取得了最佳性能。

数据集按照7:2:1的比例划分为训练集、验证集与测试集，其中训练集包含约6700个样本，验证集与测试集分别包含约1835个样本。数据划分遵循时间顺序，确保验证集与测试集中的样本时间戳晚于训练集，以模拟真实的时序预测场景。

#### 3.2.3 统一预处理流水线

针对时序信号的预处理，本系统建立了统一的处理流水线，包含去噪、归一化与异常段检测三个关键步骤。去噪模块采用低通滤波器与异常值检测相结合的方式，首先通过截止频率为50Hz的Butterworth低通滤波器去除高频测量噪声，随后使用基于统计学原理的3σ准则识别并处理突发的异常测量值。

归一化采用Z-Score标准化方法，对每个特征维度独立计算均值与标准差，公式为x_normalized = (x - μ) / σ。标准化参数仅基于训练集统计得到，验证集与测试集使用相同的参数进行变换，确保推理时处理流程的一致性。异常段检测模块通过滑动窗口计算信号的局部方差，当局部方差超过全局阈值的一定倍数时将该窗口标记为异常段。异常段信息同时输出供后续分析使用，在模型预测时对异常段给予特殊处理以提升鲁棒性。

### 3.4 创新模块A：MoE混合专家与freqMOE频域增强

本项目提出的第一项核心创新是MoE（Mixture of Experts）混合专家增强模块。该模块的核心思想是利用多个专家网络分别处理信号的不同频率分量，通过可学习的门控机制动态分配权重，实现频率自适应的信号增强。

#### 3.4.1 MoE模块架构

模块架构包含三个主要组件：频率划分单元、专家网络集合与门控权重生成器。频率划分单元将输入信号的FFT表示分割为多个不相交的频段，分割边界可通过参数配置或端到端学习获得。专家网络集合包含若干独立的神经网络，每个专家负责处理特定频段内的信号分量，专家数量可根据任务复杂度调整。门控权重生成器基于输入信号的全局特征计算各专家的激活权重，实现数据驱动的专家选择。

门控机制采用温度调节的Softmax函数，公式为w_i = exp(g(x)/T) / Σexp(g_j(x)/T)，其中g(x)为门控网络的输出，T为温度参数。温度T控制权重的分布锐度，T值趋近于0时权重趋近于one-hot分布（硬选择），T值趋近于无穷时时权重趋近于均匀分布（软选择）。实验表明，适中的温度值（约0.85）能够在利用专家特化优势与保持系统灵活性之间取得平衡。

输出融合采用加权求和方式，将各专家的处理结果按门控权重进行融合。为增强系统的鲁棒性，模块同时保留一定比例的原始信号作为残差连接，残差比例通过参数配置。

#### 3.4.2 freqMOE频域增强模块

在MoE框架的基础上，本项目进一步提出了freqMOE频域增强模块，专门针对时序信号的频域特性进行优化。该模块的核心创新在于将信号转换到频域后，通过可学习的频率掩码（Frequency Mask）实现可微的频段划分，而非传统的离散频段分割。

freqMOE支持两种频率掩码模式：硬掩码（Hard Mask）与软掩码（Soft Mask）。硬掩码使用可学习的切割点参数将频谱划分为若干离散区间，每个专家负责对应区间的信号处理。软掩码则使用可学习的中心频率与带宽参数，通过sigmoid函数生成平滑的频率响应曲线，实现频段之间的软过渡。

为解决多专家场景下的专家塌缩问题，freqMOE引入了负载均衡损失（Load Balancing Loss），其公式为L_balance = Σ(weight_mean - 1/K)²，其中K为专家数量，weight_mean为各专家的平均激活权重。该损失项鼓励专家被均匀激活，避免单个专家主导的情况。

#### 3.4.3 针对低频抖动数据的优化

针对手机抖动数据的特殊频率特性（99.99%能量集中在0-10Hz低频段），本项目设计了一套专门的优化策略。首先是细粒度低频专家配置，通过expert_freq_range参数将专家的频率作用范围限制在低频区间，使多个专家能够在有限的低频范围内进行更精细的分工。其次是Soft Mask与负载均衡损失的组合使用，实验表明该组合能够有效缓解专家塌缩问题（从90%+的单一专家主导改善至69%/14%/9%/8%的多专家分布）。

然而，消融实验揭示了一个重要发现：对于低频为主的信号，1专家配置（即不使用频段划分，仅保留FFT域的归一化处理）反而取得了最佳效果。这是因为1专家本质上实现了一种"FFT域归一化"增强，对低频数据特别有效，而强行进行频段划分会引入额外的噪声与误差。基于这一发现，最终的最优配置采用1专家+更强数据增强的策略，在手机抖动数据集上取得了显著的性能提升。

### 3.6 反向补偿与稳像生成

预测得到的未来抖动轨迹需要经过反向补偿才能生成最终的稳像输出。本模块包含轨迹平滑、过补偿抑制、裁切策略与时延控制四个子模块。

轨迹平滑采用Savitzky-Golay滤波器与自适应窗口相结合的方式。滤波器阶数与窗口大小根据预测序列的长度与噪声特性自适应调整，在保留原始运动趋势的前提下尽可能消除高频抖动分量。过补偿抑制模块通过分析预测轨迹与历史轨迹的连续性，检测可能出现过度修正的段落并自动调整补偿强度。当预测轨迹的变化率超过设定阈值时，系统自动降低该段的补偿系数以避免引入新的失真。

裁切策略是稳像输出的关键环节。由于预测的抖动轨迹描述的是未来时刻的偏移量，需要将其转换为图像空间的裁切矩阵才能实际应用。裁切矩阵的计算公式为T_i = [1, 0, Δx_i; 0, 1, Δy_i; 0, 0, 1]，其中Δx_i与Δy_i为第i帧在x与y方向的位移分量。裁切窗口的大小根据整段预测的最大抖动幅度确定，确保稳像后的图像内容完整不丢失。

时延控制模块平衡预测精度与系统实时性。较长的预测步长能够提前更多时间进行补偿，但预测误差也会相应增大。系统根据应用场景需求支持可配置的预测步长，在手机稳像场景中默认使用200步（约400ms）的预测长度，在需要更低延迟的应用中可缩短至100步。

### 3.7.1 稳像指标定义

稳像效果的量化评估采用以下核心指标。均方根误差（RMSE）衡量预测轨迹与真实轨迹之间的整体偏差，定义为RMSE = √(Σ(y_pred - y_true)² / n)。平均绝对误差（MAE）反映预测误差的绝对水平，定义为MAE = Σ|y_pred - y_true| / n，对异常值不如RMSE敏感。

此外引入频域指标用于评估稳像后信号的频率分布。信号能量比（SER）定义为稳像前后低频能量之比，SER = E_low( stabilized ) / E_low( original )，用于衡量低频抖动分量的抑制效果。频率响应一致性（FRC）衡量稳像输出与原始运动趋势在频域的相似程度，FRC = corr( FFT(stabilized)[0:f_c], FFT(trend)[0:f_c] )，其中f_c为关键频率阈值。

### 3.7.3 导出与复现记录

系统支持多种格式的导出以满足不同应用场景需求。数值矩阵导出采用JSON格式，包含完整的输入信号、预测轨迹、补偿矩阵与置信度得分，支持下游程序的自动化处理。统计报告导出采用CSV格式，包含各评估指标的计算结果与样本级别的质量评级。可视化图像导出采用PNG格式，包含轨迹对比图、频谱分析图与异常段标记图。

复现记录功能确保实验结果的可追溯性。每次推理自动记录模型版本、参数配置、随机种子与计算环境信息，形成完整的元数据文件。元数据与输出结果绑定存储，支持基于配置条件的精确复现与跨版本对比分析。

## 第四章 评估与结果分析

### 4.3 消融实验

为验证MoE混合专家模块与频域增强策略的贡献，本节设计并实施了一系列消融实验。实验在手机抖动数据集上以PatchTST为主干网络，使用相同的训练参数与数据划分进行对比。

#### 4.3.1 MoE模块与频域增强基线对比

首先进行的是MoE模块的去除实验。基准配置使用完整MoE增强（9个频率专家，top-3激活），标准化MAE为0.0939。去除MoE模块后仅使用PatchTST主干的基线配置，MAE上升至0.0978，相对降幅4.0%。这一结果表明MoE模块对预测精度具有稳定的提升作用，其收益来自于频率自适应处理对不同抖动模式的适配能力。

其次验证频域增强策略的影响。分别测试了FFT域增强（使用预设的频段权重）、freqMOE多专家（4专家，频段划分）与freqMOE单专家（1专家，FFT归一化）三种配置。FFT域增强的MAE为0.0932，freqMOE多专家的MAE为0.1218，freqMOE单专家的MAE为0.0892。值得注意的是，freqMOE单专家配置（即不使用频段划分，仅保留FFT域的归一化处理）取得了最佳的预测效果，这一发现与数据的频率特性密切相关。手机抖动数据99.99%的能量集中在低频段，强行进行频段划分反而会引入额外的噪声与误差。

#### 4.3.2 专家数量敏感性分析

专家数量的敏感性实验揭示了重要的规律。专家数量从1增加到2时MAE从0.0892上升至0.1234，升幅达38%。继续增加专家数量至4与8，MAE进一步恶化至0.1218与0.1325。这一现象验证了之前的推断：对于低频为主的信号，频段划分的策略收益为负，增加专家数量仅会引入更多需要学习的参数而无法获得对应的频域特化收益。

#### 4.3.3 细粒度低频专家实验

为探索在主导专家频段内进行更细划分的可能性，本项目进一步测试了细粒度低频专家配置。实验将4个专家限制在20%的低频范围内（每个专家约5%的覆盖），期望实现更精细的频率分工。然而，实验结果显示该配置的MAE为0.1188，虽比全频段4专家（0.1218）略有改善，但仍远差于1专家配置。

进一步增加专家数量至8个并缩专家频率范围至10%，MAE进一步恶化至0.1260与0.1261。实验表明，网络在训练过程中会自动将专家的频率响应扩展到全频谱范围，而非保持在初始的低频区间内，这说明低频范围的信号变化不足以支撑多个专家的有效分工。

#### 4.3.4 Soft Mask与负载均衡损失效果

为解决多专家场景下的专家塌缩问题，实验测试了Soft Mask与负载均衡损失的组合使用。结果显示：

- Hard Mask 4专家：专家权重分布为90%/10%/0%/0%（严重塌缩）
- Soft Mask + Balance Loss 4专家：专家权重分布改善为69%/14%/9%/8%

虽然专家分布得到显著改善，但MAE仍为0.1188，未能超越1专家配置。这进一步证实了"针对低频数据，1专家最优"的结论。

#### 4.3.5 数据增强与训练策略优化

在1专家配置基础上，进一步探索数据增强与训练策略的优化。增加数据增强强度（jitter_sigma: 0.08→0.12，scale_alpha: 0.1→0.15）与训练轮数（8→12轮）后，最优配置达到了标准化MAE=0.0841，相较于原始1专家配置（0.0892）提升5.7%，相较于基准模型（0.0978）提升14.1%。

预测步长的敏感性分析结果显示，随着预测步长从100增加至200，预测误差呈上升趋势但整体可控。在100步预测时MAE约为0.075，200步预测时MAE约为0.090，400步预测时MAE约为0.115。误差增长的幅度与步长呈近似线性关系，表明模型在不同预测 horizon 下均能保持合理的预测能力。

#### 4.3.6 消融实验总结

综合以上消融实验，得出以下关键结论：

| 配置                   | 标准化MAE | 相对基准提升 |
| ---------------------- | --------- | ------------ |
| freqMOE 1专家+更强增强 | 0.0841    | 14.1%        |
| freqMOE 1专家          | 0.0892    | 8.8%         |
| MoE+FFT组合            | 0.0911    | 6.8%         |
| MoE                    | 0.0939    | 4.0%         |
| FFT增强                | 0.0932    | 4.7%         |
| Baseline               | 0.0978    | -            |

实验表明，针对手机抖动这类低频主导的信号，最有效的策略是使用1专家配置配合FFT域归一化，并辅以更强的数据增强。多专家策略虽在理论上具有频率特化优势，但在实际应用中因数据频率特性过于单一而无法发挥预期效果。

### 4.5 失败复盘与归因分析

尽管系统在大多数样本上取得了满意的稳像效果，但仍存在一定比例的失败案例。通过对测试集预测结果的分析，我们将失败案例归纳为以下几类并提出相应的对策。

第一类失败是极端运动导致的预测失效。当输入信号包含大幅度的快速运动（如突然的甩动或快速转向）时，预测模型难以准确捕捉运动趋势，导致补偿轨迹与实际抖动之间出现较大偏差。对策是引入运动幅度检测模块，当检测到极端运动模式时自动切换至保守的裁切策略，减小裁切幅度以避免内容丢失。

第二类失败是低光环境下的信号质量问题。在光照不足的采集条件下，传感器噪声增大导致输入信号的信噪比下降，影响预测精度。对策是在预处理阶段增加信号质量评估，根据噪声水平自适应调整滤波参数与预测置信度阈值。

第三类失败是过补偿引入的伪影。当预测的抖动轨迹存在系统性偏差时，补偿后的输出可能呈现出与原始抖动方向相反的异常模式。对策是增加轨迹连续性约束，在优化目标中加入相邻帧之间的平滑项，限制补偿量突变的发生。

第四类失败是模型泛化能力的局限。当前模型基于特定采集设备与采集场景的数据训练，在换用其他型号手机或改变握持方式后性能可能出现下降。对策是收集更多样化的训练数据并引入域适应技术，提升模型的跨设备泛化能力。

## 第五章 产品化与应用前景

### 5.2 手机影像应用场景

手机是本系统最直接的应用载体。在短视频拍摄场景中，手持手机进行录制时难以避免的微小抖动会显著影响观看体验，尤其在放大画面或快速运动时更为明显。本系统通过实时预测未来数帧的抖动轨迹，可在拍摄过程中提前进行补偿，有效提升成片的稳定性。对于直播场景，系统支持低延迟模式，在保证稳像效果的前提下将处理时延控制在可接受范围内。

运动拍摄是另一个重要场景。用户在跑步、骑行或进行其他剧烈运动时拍摄视频，抖动幅度远超日常手持场景。针对这一场景，系统预留了运动模式参数集，通过调整预测步长、补偿系数与裁切策略来适应大幅度的抖动模式。运动模式的启用可由用户手动选择或由系统根据传感器信号自动判断。

直播带货与视频会议是新兴的手机稳像应用方向。在直播过程中，主播需要频繁移动位置或转换视角，传统的光学防抖与电子防抖难以应对这种复杂的抖动模式。本系统的预测式稳像方法能够提前预估抖动趋势并进行补偿，在保证画面稳定的同时保留自然的运动感，避免过度平滑导致的“死板”视觉效果。

### 5.4 风险与对策

手机稳像技术在实际应用中面临多重风险与挑战。算力风险是首要考虑因素，移动设备的计算资源有限，复杂的深度学习模型难以在端侧实时运行。当前系统采用模型轻量化策略，通过知识蒸馏、量化压缩与剪枝技术将模型参数量降低至可接受范围。实验表明，经过4倍量化压缩的模型在主流手机上仍能保持80%以上的原始精度，推理速度提升至30fps以上，满足实时处理需求。

过补偿风险是预测式稳像方法的固有挑战。当预测误差较大或模型对当前运动模式判断不准确时，补偿后的画面可能出现往复抖动或画面漂移。系统在输出端增加了过补偿检测模块，当检测到补偿量异常或连续多帧补偿方向一致时自动降低补偿强度或回退至原始图像，确保输出质量的下限。

极端场景风险包括超低光环境、高速运动与剧烈抖动等边界工况。在这些条件下，输入信号的信噪比显著下降，预测模型的置信度也会相应降低。系统通过置信度机制识别低质量预测，对低置信度样本采用保守策略或提示用户改善拍摄条件。同时，系统保留了传统稳像方法作为备选，当检测到当前条件超出模型能力范围时自动切换。

数据安全风险涉及用户上传的原始视频内容的隐私保护。系统采用本地处理优先的设计理念，所有推理过程均在用户设备本地完成，不涉及云端传输。对于必须使用云端处理的场景，系统提供数据脱敏选项，在上传前自动移除敏感信息并采用加密传输协议。

## 第六章 总结与展望

本章针对手机与无人机平台的稳像需求，设计并实现了一套基于时序预测的抖动补偿系统。核心贡献包括：提出了MoE混合专家增强模块与freqMOE频域增强模块，通过频率自适应处理提升预测精度；针对低频抖动信号的特点，提出了1专家+FFT域归一化的优化策略并验证了其有效性；建立了完整的从数据采集到稳像输出的处理流水线，支持灵活的配置与扩展。

在手机抖动数据集上的实验结果表明，经过优化的freqMOE单专家配置配合更强数据增强能够将预测误差MAE降低至0.0841，相较于基准模型提升14.1%。消融实验揭示了以下关键发现：（1）专家数量与频段划分策略对不同频率特性数据的差异化影响，对于低频主导的信号，1专家配置最优；（2）Soft Mask与负载均衡损失能够有效改善专家塌缩问题，但无法改变多专家配置的性能劣势；（3）数据增强强度与训练策略对最终性能有显著影响。失败案例的分析与归因进一步明确了系统的能力边界与改进方向。

未来的工作将从以下方向展开：一是引入自监督预训练策略，利用大规模无标注数据提升模型的泛化能力；二是探索时序Transformer架构在长程预测任务上的潜力；三是研究端云协同的混合部署模式，在保证隐私安全的前提下利用云端算力处理复杂场景；四是拓展系统在无人机稳像与车载云台等更多领域的应用。

综上所述，本系统为移动设备影像稳像提供了一种有效的技术方案，兼顾了预测精度与计算效率，具有广阔的应用前景与商业价值。
